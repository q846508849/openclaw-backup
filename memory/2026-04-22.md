## 四文件对比流程（2026-04-22）

### 文件清单
| 文件 | 用途 |
|------|------|
| 按门店 YYYY-MM-DD HH_MM_SS.xlsx | 门店级原始数据 |
| 分公司网格外部数据表2025_YYYYMMDDHHMMSS.xlsx | 规划数据（负责人/2026-2028计划门店数） |
| 开店目标测算 YYYY-MM-DD HH_MM_SS.xlsx | 省级目标+完成进度 |
| 分公司测算 YYYY-MM-DD HH_MM_SS.xlsx | **基准文件**（月均/计划/完成进度） |

### 标准流程（两步走）
1. `four_file_compare_v2.py` → 生成 `新店月均销售_YYYY-MM-DD.xlsx`（我们的报告）
2. 对比报告 → 生成 `对比结果_YYYY-MM-DD.xlsx`（单Sheet，20列）

### 脚本路径
- `D:\OpenClaw\data\.openclaw\workspace\four_file_compare_v2.py`（四文件综合对比，v2版本）

### 分类规则（永久规则）
- **老店（old）**：首次营业日距今≥365天
- **新店（g180）**：首次营业日在180~365天前
- **新店（glt）**：首次营业日距今<180天
- **归组规则**：FGS_MERGE = `{'湖南郴州': '湖南区域'}`（归组先于过滤）

### 关键代码片段
```python
# FGS归组必须在过滤之前
fgs_m = merge_fgs(fgs)  # 先归组
if fgs_m not in ref_companies:  # 再过滤（分公司测算中的50个公司）

# 月均计算
def month_avg(g):
    days = g.get('days', 0)
    if days <= 0: return 0.0
    return round(g.get('sales', 0) / days * 30, 2)
```

### 输出Sheet格式（对比结果）
20列：分公司、数据来源、老店月均(3列)、新店180月均(3列)、新店Lt月均(3列)、2026计划(3列)、2027计划(3列)、2028计划(3列)
- 差值>0单元格标红（FFFFE0E0）
- 数据来源列：两者都有 / 仅我们有 / 仅参考文件有

### 今日数据（2026-04-22）
- 门店数据：6916条，0排除
- 老店5533 / 新店180~365天561 / 新店<180天606
- 50个分公司，26个有月均差值